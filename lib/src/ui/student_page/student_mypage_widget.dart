import 'package:flutter/material.dart';

class StudentMyPageWidget extends StatelessWidget {
  const StudentMyPageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('마이페이지'),
      ),
    );
  }
}
